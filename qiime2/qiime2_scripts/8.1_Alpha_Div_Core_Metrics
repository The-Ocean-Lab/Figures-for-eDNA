8.1 Alpha and Beta Diversity Core Metric

qiime diversity core-metrics-phylogenetic \
  --i-phylogeny rooted-tree.qza \
  --i-table filtered-cutoff-table.qza \
  --p-sampling-depth # \
  --m-metadata-file metadata.tsv \
  --output-dir core-metrics-results

# use the same sampling depth number as the rarefaction command
# creates core-metrics-results folder with (alpha) Shannon, Observed Features, Faith's Phylogentic Diversity, Evenness
# creates core-metrics-results folder with (beta) Jaccard Distance, Bray-Curtis Distance, Unweighted Unifrac, Weighted Unifrac
